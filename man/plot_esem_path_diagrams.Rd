% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualizations.R
\name{plot_esem_path_diagrams}
\alias{plot_esem_path_diagrams}
\title{ESEM path diagrams: BEFORE and AFTER ACO selection}
\usage{
plot_esem_path_diagrams(
  result,
  cosine_sim_matrix,
  min_loading_show = 0.1,
  show_crossloadings = TRUE,
  df = NULL,
  min_factor_cor_show = 0.05,
  max_factor_cor_edges = 15L,
  before_model = c("proxy", "refit"),
  before_refit_max_items = 60L,
  before_proxy_max_items = 150L
)
}
\arguments{
\item{result}{A high-level SEMANTICA result or output from \code{ACO_with_ESEM()}.}

\item{cosine_sim_matrix}{Full cosine similarity matrix. Optional for a high-level result because the stored matrix is reused.}

\item{min_loading_show}{Minimum absolute loading to show.}

\item{show_crossloadings}{Logical; show cross-loadings.}

\item{df}{Optional item metadata dataframe used to represent the generated
item pool in the BEFORE diagram. Without metadata, the function falls
back to the screened eligible pool stored in \code{result}.}

\item{min_factor_cor_show}{Minimum absolute latent factor correlation to
draw in the side rails.}

\item{max_factor_cor_edges}{Maximum number of factor-correlation rails.}

\item{before_model}{How to derive the BEFORE path diagram. \code{"proxy"} (the
default) uses a fast sample-free PFA/semantic proxy and never estimates a
new full-pool ESEM during plotting. \code{"refit"} requests a new ESEM fit,
subject to \code{before_refit_max_items}.}

\item{before_refit_max_items}{Maximum BEFORE-pool size for an explicitly
requested ESEM refit. This guard prevents plotting from initiating an
unexpectedly expensive full-pool estimation job. Use \code{Inf} only after
deliberately assessing the computational cost.}

\item{before_proxy_max_items}{Maximum BEFORE-pool items represented in the
default fast proxy display. Selected items are always retained; use \code{Inf}
for a complete-pool proxy.}
}
\value{
Named list of two \code{ggplot} objects (\code{p10a}, \code{p10b}).
}
\description{
ESEM path diagrams: BEFORE and AFTER ACO selection
}
\examples{
\dontrun{
paths <- plot_esem_path_diagrams(
  result, cosine_sim_matrix,
  min_loading_show = 0.10,
  show_crossloadings = TRUE,
  before_model = "proxy"
)
paths$p10b
}
}
