% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pipeline.R
\name{semantica_quality_config}
\alias{semantica_quality_config}
\title{Configure semantic and psychometric quality guardrails}
\usage{
semantica_quality_config(
  profile = c("standard", "lenient", "strict"),
  redundancy_threshold = NULL,
  dup_threshold = NULL,
  htmt_threshold = NULL,
  htmt_objective_role = c("diagnostic", "penalty"),
  cohesion_retention = NULL,
  within_similarity_target = NULL,
  within_similarity_band = NULL,
  semantic_objective_mode = c("relative_conservative", "legacy_target_burden"),
  facet_coverage_weight = NULL,
  psychometric_guard_weight = NULL,
  psychometric_guard_min_ave = NULL,
  psychometric_guard_min_loading = NULL,
  psychometric_guard_min_primary_ge_50 = NULL,
  threshold_mode = c("fixed", "adaptive_pool"),
  adaptive_redundancy_quantile = 0.95,
  adaptive_duplicate_quantile = 0.99,
  construct_blueprint = NULL,
  nomological_weight = 0,
  polarity_screen = TRUE,
  content_alignment_mode = c("diagnostic", "guard", "off"),
  polarity_action = c("diagnostic", "guard", "off"),
  within_target_method = c("nonredundant_median", "legacy_q40")
)
}
\arguments{
\item{profile}{One of \code{"standard"}, \code{"lenient"}, or \code{"strict"}.}

\item{redundancy_threshold, dup_threshold, htmt_threshold}{Optional threshold
overrides. \code{htmt_threshold} is a descriptive semantic-proxy reference by
default; it affects the optimization utility only when
\code{htmt_objective_role = "penalty"} is explicitly requested.}

\item{htmt_objective_role}{Role of the HTMT-like semantic proxy in ESEM
scoring. \code{"diagnostic"} (default) reports overlap continuously without
changing the objective; \code{"penalty"} restores threshold-based objective
influence for compatibility/sensitivity analyses.}

\item{cohesion_retention}{Candidate-pool retention around the target
within-factor similarity.}

\item{within_similarity_target, within_similarity_band}{Within-factor
semantic cohesion target and tolerance. In the default multidimensional
relative objective these act as cohesion/redundancy guards rather than as
the primary definition of semantic quality.}

\item{semantic_objective_mode}{Multidimensional semantic objective.
\code{"relative_conservative"} (default) combines threshold-free stochastic
superiority with a robust within-between median-gap component.
\code{"legacy_target_burden"} retains the 0.4.x score for reproducibility.}

\item{facet_coverage_weight}{Soft weight for facet coverage.}

\item{psychometric_guard_weight}{Soft penalty strength for weak proxy
structure.}

\item{psychometric_guard_min_ave, psychometric_guard_min_loading, psychometric_guard_min_primary_ge_50}{Minimum proxy-structure guardrails.}

\item{threshold_mode}{\code{"fixed"} preserves declared thresholds; \code{"adaptive_pool"} enables an explicitly experimental pool-relative heuristic.}

\item{adaptive_redundancy_quantile, adaptive_duplicate_quantile}{Quantiles used by the experimental pool-relative threshold calibration.}

\item{construct_blueprint}{Optional object from \code{\link[=semantica_construct_blueprint]{semantica_construct_blueprint()}}.}

\item{nomological_weight}{Optional theory-alignment weight for matching specified \emph{semantic-domain} factor relations; defaults to zero.}

\item{polarity_screen}{Run conservative reverse/negation wording diagnostics.}

\item{content_alignment_mode}{\code{"diagnostic"} (default) reports item-to-definition
alignment without excluding candidates; \code{"guard"} applies a conservative,
pool-relative guard that removes only clear factor mismatches or explicit
exclusion conflicts when enough alternatives remain; \code{"off"} disables it.}

\item{polarity_action}{\code{"guard"} excludes flagged wording when enough
alternatives remain, \code{"diagnostic"} only reports it, and \code{"off"} disables
selection guarding.}

\item{within_target_method}{Automatic within-factor target method when
\code{within_similarity_target = NULL}. \code{"nonredundant_median"} uses the median
within-factor similarity below the configured redundancy threshold in the
current pool/model; \code{"legacy_q40"} reproduces the older 0.25--0.55-clamped
40th-percentile heuristic for compatibility studies.}
}
\value{
A configuration list for \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}}.
}
\description{
Configure semantic and psychometric quality guardrails
}
