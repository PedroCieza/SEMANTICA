% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_generation.R
\name{semantica_backend_spec}
\alias{semantica_backend_spec}
\title{Define an explicit custom SEMANTICA backend contract}
\usage{
semantica_backend_spec(
  protocol,
  label = "Custom backend",
  chat_url = NULL,
  embed_url = NULL,
  can_chat = !is.null(chat_url),
  can_embed = !is.null(embed_url),
  supports_structured_output = FALSE,
  supports_batch_embeddings = TRUE,
  default_chat_model = NULL,
  default_embed_model = NULL,
  embed_dim = NA_integer_,
  auth_header = NULL,
  auth_env = NULL,
  extra_headers = NULL
)
}
\arguments{
\item{protocol}{Supported transport protocol: \code{"openai_compat"},
\code{"anthropic"}, \code{"ollama"}, \code{"python_hf"}, or \code{"python_llamacpp"}.}

\item{label}{Human-readable backend label.}

\item{chat_url, embed_url}{Explicit service endpoints where applicable.}

\item{can_chat, can_embed}{Declared capabilities.}

\item{supports_structured_output, supports_batch_embeddings}{Capability flags.}

\item{default_chat_model, default_embed_model}{Optional model defaults.}

\item{embed_dim}{Optional documented embedding dimension; \code{NA} means unknown.}

\item{auth_header, auth_env}{Authentication metadata. Credentials themselves
are never stored in the specification.}

\item{extra_headers}{Optional non-secret protocol headers.}
}
\value{
A validated \code{semantica_backend_spec} object.
}
\description{
Creates an immutable transport/capability specification for a custom service
that uses one of SEMANTICA's supported wire protocols. This avoids provider-
name guessing and keeps provider transport metadata outside the analysis
core.
}
