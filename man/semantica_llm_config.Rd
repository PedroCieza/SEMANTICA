% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pipeline.R
\name{semantica_llm_config}
\alias{semantica_llm_config}
\title{Configure LLM providers for the configurable pipeline}
\usage{
semantica_llm_config(
  backend = "openai",
  embed_backend = NULL,
  api_key = NULL,
  embed_api_key = NULL,
  hf_token = NULL,
  embed_hf_token = NULL,
  base_url = NULL,
  embed_base_url = NULL,
  gguf_path = NULL,
  embedding_device = "auto",
  chat_device = "auto",
  device_map = NULL,
  gpu_layers = "auto",
  model_precision = "auto",
  embed_batch_size = 64L,
  embedding_cache = TRUE,
  embedding_cache_dir = NULL,
  embedding_cache_namespace = NULL,
  retry_max_tries = 4L,
  retry_on_failure = TRUE,
  preflight = TRUE,
  timeout_s = 120L,
  embed_timeout_s = NULL,
  release_local_models = FALSE,
  embedding_task = "auto",
  embedding_instruction = NULL,
  embedding_spec = NULL,
  backend_spec = NULL,
  embed_backend_spec = NULL
)
}
\arguments{
\item{backend}{Generation backend. Accepted registered values are \code{"openai"},
\code{"anthropic"}, \code{"groq"}, \code{"ollama"}, \code{"llamacpp"},
\code{"generic_openai"}, \code{"python_hf"}, and \code{"python_llamacpp"}.}

\item{embed_backend}{Embedding backend using the same registered names as
\code{backend}. \code{NULL} reuses \code{backend}; use a separate value when the chat
provider does not supply embeddings.}

\item{api_key, embed_api_key}{Provider keys for generation and embedding.}

\item{hf_token, embed_hf_token}{Hugging Face tokens for local model sessions.}

\item{base_url}{Optional provider/server URL for generation, including
OpenAI-compatible providers not registered in \code{SEMANTICA_BACKENDS}.}

\item{embed_base_url}{Optional provider/server URL for embeddings.}

\item{gguf_path}{Optional GGUF path for local llama.cpp workflows.}

\item{embedding_device, chat_device, device_map, gpu_layers, model_precision}{Local-model deployment controls.}

\item{embed_batch_size}{Items per embedding request.}

\item{embedding_cache}{Logical; use persistent content-addressed embedding caching.}

\item{embedding_cache_dir}{Optional directory for persistent embedding cache files.}

\item{embedding_cache_namespace}{Optional analyst-defined namespace included in cache keys.}

\item{retry_max_tries}{Maximum number of attempts for retryable HTTP requests.}

\item{retry_on_failure}{Logical; retry transient HTTP/provider failures when supported.}

\item{preflight}{Logical; validate configured provider/model capabilities before network work.}

\item{timeout_s, embed_timeout_s}{HTTP timeout in seconds for generation and
optional separate embedding sessions.}

\item{release_local_models}{Release cached local Python models after use.}

\item{embedding_task}{Embedding-task policy. Accepted values are \code{"auto"},
\code{"psychometric_similarity"}, \code{"clustering"}, \code{"classification"},
\code{"search_document"}, \code{"search_query"}, and \code{"none"}. \code{"auto"} is
recommended: it applies a documented model-specific instruction only when
SEMANTICA knows that the configured embedding family requires one.}

\item{embedding_instruction}{Optional explicit embedding prefix/instruction; overrides automatic model-specific instructions.}

\item{embedding_spec}{Optional embedding capability contract from
\code{\link[=semantica_embedding_spec]{semantica_embedding_spec()}}. Use this for an unregistered model whose
documented task-instruction behavior is known.}

\item{backend_spec, embed_backend_spec}{Optional explicit custom backend contracts created by \code{\link[=semantica_backend_spec]{semantica_backend_spec()}}.}
}
\value{
A configuration list for \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}}.
}
\description{
Groups provider, credential, server, and local-model deployment settings.
Model names remain explicit arguments of \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}} so users
can choose generation and embedding models directly.
}
