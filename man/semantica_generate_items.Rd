% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_generation.R
\name{semantica_generate_items}
\alias{semantica_generate_items}
\title{Generate psychometric items for all factors using an LLM}
\usage{
semantica_generate_items(
  session,
  scale_name,
  scale_description,
  factors,
  response_format = "5-point Likert",
  item_style = "first-person declarative sentence",
  language = "English",
  n_per_factor = NULL,
  n_per_factor_override = !is.null(n_per_factor),
  overgenerate = 2,
  max_retries = 3L,
  global_forbidden_max = 40L,
  temperature = 0.8,
  verbose = TRUE
)
}
\arguments{
\item{session}{A \code{semantica_session} from \code{semantica_connect()}.}

\item{scale_name}{Short name of the scale.}

\item{scale_description}{One-paragraph construct description.}

\item{factors}{Named list of dimension specs (\verb{$description}, \verb{$n_items},
optional \verb{$facets}, \verb{$examples}, \verb{$forbidden}, \verb{$extra_instructions}).}

\item{response_format}{e.g., \code{"5-point Likert"}.}

\item{item_style}{e.g., \code{"first-person declarative sentence"}.}

\item{language}{Language for item text.}

\item{n_per_factor}{Global target number of retained items per factor.}

\item{n_per_factor_override}{Logical; when \code{TRUE} and \code{n_per_factor} is
supplied, distribute that total across facets even when the factor
specification contains facet-level item counts. Defaults to \code{TRUE} for
direct calls that supply \code{n_per_factor}.}

\item{overgenerate}{LLM overgeneration multiplier.}

\item{max_retries}{Retries on short responses.}

\item{global_forbidden_max}{Maximum number of previously generated items to
include as anti-duplicate examples in subsequent prompts.}

\item{temperature}{LLM temperature.}

\item{verbose}{Print progress.}
}
\value{
Tibble with legacy columns (\code{item_id}, \code{factor}, \code{item_text},
\code{attempt}) and facet-aware columns (\code{ID}, \code{Dimension}, \code{Facet}, \code{item}).
}
\description{
Generate psychometric items for all factors using an LLM
}
\examples{
\dontrun{
session <- semantica_connect("openai")
factors <- list(
  Clarity = list(n_items = 8L, description = "Clear thinking."),
  Flexibility = list(n_items = 8L, description = "Adaptive thinking.")
)
items <- semantica_generate_items(
  session = session,
  scale_name = "Cognitive Agility",
  scale_description = "Clear and adaptive thinking.",
  factors = factors,
  n_per_factor_override = FALSE,
  verbose = FALSE
)
}
}
