% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pipeline.R
\name{semantica_item_count_config}
\alias{semantica_item_count_config}
\title{Configure generated-pool and selected-form item counts}
\usage{
semantica_item_count_config(
  pool = 15L,
  selected = 3L,
  override_pool_counts = TRUE
)
}
\arguments{
\item{pool}{Positive integer number of retained candidate items per factor.
This is the post-generation target pool size, not necessarily the number
of raw items requested from the LLM when \code{overgenerate > 1}.}

\item{selected}{Number of final selected items per factor. A scalar is
recycled across factors; a named vector can set factor-specific targets.}

\item{override_pool_counts}{Logical. If \code{TRUE}, \code{pool} replaces item counts
nested inside factor/facet specifications and is allocated across facets.
If \code{FALSE}, nested counts are preserved.}
}
\value{
A configuration list for \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}}.
}
\description{
Defines candidate-pool and final-form sizes for the advanced high-level
pipeline. The common \code{pool_items} and \code{selected_items} controls can also be set
directly in \code{\link[=semantica_run]{semantica_run()}}.
}
