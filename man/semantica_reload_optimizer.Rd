\name{semantica_reload_optimizer}\alias{semantica_reload_optimizer}\title{Reload an optimizer-interchange export}\usage{semantica_reload_optimizer(prefix = "SEMANTICA", i.per.f = NULL, default_i_per_f = 3L)}\arguments{\item{prefix,i.per.f,default_i_per_f}{Passed directly to \code{semantica_reload()}.}}\description{Descriptive alias for \code{semantica_reload()}. It reads the component-level CSV interchange format and does not reconstruct high-level report exports.}\value{The optimizer-input list returned by \code{semantica_reload()}.}
