% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_generation.R
\name{semantica_export}
\alias{semantica_export}
\title{Export pipeline results to CSV files}
\usage{
semantica_export(pipeline_result, prefix = "SEMANTICA")
}
\arguments{
\item{pipeline_result}{Output list from \code{\link{semantica_pipeline}} or \code{\link{semantica_wrap}}.}

\item{prefix}{Character string to prepend to output filenames.}
}
\value{
Invisibly returns the prefix used.
}
\description{
Saves the generated item table, metadata dataframe, and cosine similarity
matrix to separate CSV files in the working directory for offline use or sharing.
}
\examples{
\donttest{
items_tbl <- data.frame(
  item_id = paste0("item_", 1:6),
  factor = rep(c("A", "B"), each = 3),
  item_text = paste("Item", 1:6)
)
embeddings <- diag(6)
rownames(embeddings) <- items_tbl$item_id
wrapped <- semantica_wrap(
  list(
    embeddings = embeddings,
    items_tbl = items_tbl,
    embed_model = "manual",
    embedding_diagnostics = list(normalized = TRUE)
  ),
  verbose = FALSE
)
semantica_export(wrapped, prefix = file.path(tempdir(), "example_scale"))
}
}
