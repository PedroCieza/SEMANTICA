% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pipeline.R
\name{semantica_fit_calibration_config}
\alias{semantica_fit_calibration_config}
\title{Configure DFI and fit-calibration behavior}
\usage{
semantica_fit_calibration_config(
  mode = c("auto", "semantic_roc_dfi", "semantic_approx_dfi", "esem_parametric_dfi",
    "strict_cfa_dfi", "heuristic_semantic", "off", "fast", "strict"),
  reps = 500L,
  level = 1,
  criterion = "Sensitivity",
  esem_reps = NULL,
  search_reps = NULL,
  final_recalibrate = FALSE,
  final_reps = NULL,
  roc_misspec_strength = 1,
  strategy = c("fixed", "adaptive"),
  adaptive_min_reps = NULL,
  adaptive_batch_reps = 50L,
  adaptive_tol = 0.002,
  adaptive_stable_batches = 2L,
  fallback_policy = c("conservative", "requested_only"),
  data_type = "continuous",
  target_loadings = 0.7,
  target_factor_cors = NULL,
  loading_pattern = "varied",
  embed_reliability = 1,
  residual_inflation = 0,
  warmup_iters = 5L
)
}
\arguments{
\item{mode}{Calibration mode. \code{"off"} and \code{"fast"} both map to
\code{dfi_mode = "heuristic_semantic"}; \code{"strict"} maps to
\code{dfi_mode = "esem_parametric_dfi"}.}

\item{reps, level, criterion}{Search-time DFI simulation controls.}

\item{esem_reps, search_reps}{Optional ESEM-DFI replication budgets.}

\item{final_recalibrate, final_reps}{Optional final DFI recalibration.}

\item{roc_misspec_strength}{Semantic-ROC misspecification strength.}

\item{strategy}{ESEM-DFI simulation strategy: \code{"fixed"} uses the requested
replication budget; \code{"adaptive"} evaluates batches until the configured
stability rule is reached or the available budget is exhausted.}

\item{adaptive_min_reps, adaptive_batch_reps, adaptive_tol, adaptive_stable_batches}{Adaptive ESEM DFI controls.}

\item{fallback_policy}{DFI fallback policy: \code{"conservative"} permits the
documented safer fallback sequence when a requested calibration cannot be
estimated; \code{"requested_only"} does not substitute another DFI method.}

\item{data_type}{Data-type label used by DFI/ESEM logic. The established
workflow recognizes \code{"continuous"}, \code{"likert"}, \code{"categorical"}, and
\code{"nonnormal"}; some DFI paths require \code{original_data} for non-continuous
assumptions and otherwise fall back transparently.}

\item{target_loadings, target_factor_cors, loading_pattern, embed_reliability, residual_inflation}{Population assumptions for fallback DFI.}

\item{warmup_iters}{Warm-up iterations before DFI calibration.}
}
\value{
A configuration list for \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}}.
}
\description{
Advanced controls for SEMANTICA's established fit-calibration and DFI paths.
ACO presets provide coherent defaults for these calibration-level controls
when individual tuning is not required.
}
