% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_generation.R
\name{semantica_import_embeddings}
\alias{semantica_import_embeddings}
\title{Import externally produced embeddings into SEMANTICA}
\usage{
semantica_import_embeddings(
  embeddings,
  items_tbl,
  id_col = "item_id",
  embedding_ids = rownames(embeddings),
  normalize = FALSE,
  provider = "external",
  model = NULL,
  model_version = NULL,
  task = NULL,
  instruction = NULL,
  source = "external",
  provenance = NULL
)
}
\arguments{
\item{embeddings}{Numeric matrix with one row per item and any positive
embedding dimension.}

\item{items_tbl}{Data frame containing item identity/metadata.}

\item{id_col}{Item-ID column in \code{items_tbl}.}

\item{embedding_ids}{Explicit row IDs for \code{embeddings}. Defaults to matrix
row names. Positional alignment is intentionally not guessed.}

\item{normalize}{Logical; explicitly L2-normalize imported rows.}

\item{provider, model, model_version, task, instruction, source}{Optional safe
representation provenance. Unknown values may be \code{NULL}.}

\item{provenance}{Optional additional named provenance fields. Credential-like
fields are removed by SEMANTICA's provenance sanitizer.}
}
\value{
A \code{semantica_embedding_result} list compatible with \code{\link[=semantica_wrap]{semantica_wrap()}}.
}
\description{
Creates the same validated embedding-result contract consumed by
\code{\link[=semantica_wrap]{semantica_wrap()}} without requiring a SEMANTICA provider session. This is
the preferred boundary for embeddings produced by Python, another R package,
a corporate inference service, or a future/custom model.
}
