% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_generation.R
\name{semantica_wrap}
\alias{semantica_wrap}
\title{Prepare cosine_sim_matrix and df for SEMANTICA optimizers}
\usage{
semantica_wrap(
  embed_result,
  items_tbl = NULL,
  id_col = "item_id",
  factor_col = "factor",
  min_items_per_factor = 3L,
  cosine_adjustment = c("none", "mean_center"),
  semantic_calibration = NULL,
  compute_cosine_sensitivity = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{embed_result}{Output of \code{semantica_embed()}.}

\item{items_tbl}{Override tibble.}

\item{id_col}{Item identifier column.}

\item{factor_col}{Factor assignment column.}

\item{min_items_per_factor}{Factors with fewer items are dropped.}

\item{cosine_adjustment}{\code{"none"} for standard cosine or \code{"mean_center"}
for a centroid-subtracted anisotropy sensitivity check.}

\item{semantic_calibration}{Optional square matrix or function used to
calibrate the embedding cosine proxy before ACO/ESEM. A calibration
function receives the cosine matrix plus \code{item_metadata} and
\code{factor_assignment}; the result must preserve matrix dimensions.}

\item{compute_cosine_sensitivity}{Logical; compute the additional
none-versus-mean-centered cosine sensitivity diagnostic. Set to \code{FALSE}
when only the selected cosine representation is required and memory is
constrained; this does not affect the matrix supplied to optimization.}

\item{verbose}{Print summary.}
}
\value{
Named list: \verb{$cosine_sim_matrix}, \verb{$df}, \verb{$i.per.f} + metadata.
}
\description{
Prepare cosine_sim_matrix and df for SEMANTICA optimizers
}
\examples{
items_tbl <- data.frame(
  item_id = paste0("item_", 1:6),
  factor = rep(c("Clarity", "Flexibility"), each = 3),
  item_text = paste("Example item", 1:6)
)
embeddings <- matrix(
  c(
    0.90, 0.10, 0.20,
    0.88, 0.12, 0.18,
    0.86, 0.15, 0.22,
    0.15, 0.88, 0.25,
    0.12, 0.86, 0.28,
    0.18, 0.84, 0.23
  ),
  nrow = 6,
  byrow = TRUE,
  dimnames = list(items_tbl$item_id, NULL)
)
embed_result <- list(
  embeddings = embeddings,
  items_tbl = items_tbl,
  embed_model = "manual-example",
  embedding_diagnostics = list(normalized = FALSE)
)
wrapped <- semantica_wrap(embed_result, verbose = FALSE)
names(wrapped)
}
