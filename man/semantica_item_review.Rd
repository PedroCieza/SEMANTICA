\name{semantica_item_review}\alias{semantica_item_review}\title{Consolidate item-level diagnostics for review}\usage{semantica_item_review(result, scope = c("selected", "pool"))}\arguments{\item{result}{A SEMANTICA result.}\item{scope}{Review selected items or the retained candidate pool.}}\description{Rearranges already-computed item metadata and available semantic, ESEM, and PFA diagnostics into one table. It introduces no new score or threshold.}\value{A data frame with one row per reviewed item.}
