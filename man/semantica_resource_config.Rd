% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pipeline.R
\name{semantica_resource_config}
\alias{semantica_resource_config}
\title{Configure CPU use for SEMANTICA}
\usage{
semantica_resource_config(
  cpu_cores = "auto",
  reserve_cpu_cores = 1L,
  max_cpu_cores = NULL
)
}
\arguments{
\item{cpu_cores}{How many CPU cores SEMANTICA may use. Use \code{"auto"} to let
SEMANTICA resolve an allocation-, coordinator-, physical-core-, and
memory-aware PSOCK worker count, or \code{1} for serial execution. Explicit
numeric values remain user-authoritative. The
compatibility aliases \code{"serial"}, \code{"none"}, and \code{"off"} also request
serial execution.}

\item{reserve_cpu_cores}{CPU cores to leave unused when \code{cpu_cores = "auto"}. The main/coordinator R process is budgeted separately.}

\item{max_cpu_cores}{Optional hard ceiling on CPU workers.}
}
\value{
A configuration list for \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}}.
}
\description{
Controls the existing worker-allocation policy used by the advanced pipeline.
The \code{workers} argument of \code{\link[=semantica_run]{semantica_run()}} covers the common worker-control use case.
}
