% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_experience.R
\name{semantica_view}
\alias{semantica_view}
\alias{print.semantica_result_view}
\alias{print.semantica_result_section}
\title{Present a completed SEMANTICA result without expanding the full raw list}
\usage{
semantica_view(
  result,
  view = c("auto", "compact", "advanced", "raw"),
  section = NULL
)

\method{print}{semantica_result_view}(x, ...)

\method{print}{semantica_result_section}(x, ...)
}
\arguments{
\item{result}{A \code{semantica_full_pipeline_result} or compatible SEMANTICA
high-level result.}

\item{view}{One of \code{"auto"}, \code{"compact"}, \code{"advanced"}, or \code{"raw"}.}

\item{section}{Optional advanced section: \code{"scale"}, \code{"generation"},
\code{"content"}, \code{"semantic"}, \code{"structural"}, \code{"optimization"},
\code{"evidence"}, \code{"outputs"}, or \code{"provenance"}. A future-version
\code{"other"} section is exposed automatically if new top-level fields have
not yet been assigned to a named presentation section.}

\item{x}{A result-view or result-section object returned by
\code{semantica_view()}.}

\item{...}{Additional print arguments; currently ignored.}
}
\value{
A compact \code{semantica_result_view}, an advanced component-map view,
a read-only subset of original stored components for one section, or the
canonical full result when \code{view = "raw"}.
}
\description{
\code{semantica_view()} is a read-only presentation layer for completed SEMANTICA
results. Direct \code{semantica_full_pipeline()} output remains unchanged. For
\code{semantica_run()}, the canonical full result is retained unchanged under the
facade's \code{advanced} field, while the top level is intentionally compact.
}
\details{
With \code{view = "auto"}, results created by \code{\link[=semantica_run]{semantica_run()}} use the compact
scale-development view, while direct \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}} results use
the advanced component map. \code{view = "advanced"} groups all retained
top-level fields into a small set of task-oriented sections. Supplying
\code{section} returns the original stored components in that section, without
recomputing or simplifying their values. \code{view = "raw"} returns the
canonical full result: it is identical to a direct full-pipeline input and to
\code{result$advanced} for a compact \code{semantica_run()} result.

For \code{semantica_run()} results, the Environment pane shows a compact six-part
facade (\code{scale}, \code{items}, \code{diagnostics}, \code{plots}, \code{provenance}, \code{advanced}). The
complete canonical result is retained unchanged under \code{advanced}. Direct
\code{semantica_full_pipeline()} results remain the full advanced object.
}
\examples{
\dontrun{
result <- semantica_run(...)
semantica_view(result)
semantica_view(result, view = "advanced")
structural <- semantica_view(result, view = "advanced", section = "structural")
structural$fit_indices
}
}
