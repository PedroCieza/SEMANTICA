% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualizations.R
\name{semantica_plot_all}
\alias{semantica_plot_all}
\title{Generate all SEMANTICA diagnostic plots}
\usage{
semantica_plot_all(
  result,
  cosine_sim_matrix,
  df = NULL,
  multi_result = NULL,
  interactive_mode = c("2d", "3d"),
  save = FALSE,
  out_dir = ".",
  device = "png",
  width = 12,
  height = 8,
  dpi = 180,
  before_path_model = c("proxy", "refit"),
  before_path_refit_max_items = 60L,
  network_max_items = 200L,
  mds_max_items = 250L,
  path_proxy_max_items = 150L,
  include_interactive = TRUE,
  progress = TRUE
)
}
\arguments{
\item{result}{Output from \code{ACO_with_ESEM()}.}

\item{cosine_sim_matrix}{Full cosine similarity matrix.}

\item{df}{Item metadata dataframe.}

\item{multi_result}{Output from \code{run_multi_seed_semantica()} (optional).}

\item{interactive_mode}{\code{"2d"} or \code{"3d"} for Plot 9.}

\item{save}{Logical; save to disk.}

\item{out_dir}{Output directory.}

\item{device}{Image format (\code{"png"}, \code{"pdf"}, etc.).}

\item{width, height, dpi}{Plot dimensions.}

\item{before_path_model}{Passed to \code{\link[=plot_esem_path_diagrams]{plot_esem_path_diagrams()}} for the
BEFORE panel. Defaults to \code{"proxy"} so the diagnostic plot does not
trigger a new full-pool ESEM fit.}

\item{before_path_refit_max_items}{Maximum BEFORE-pool size for an
explicitly requested path-diagram ESEM refit.}

\item{network_max_items}{Maximum candidate-pool items rendered in Plot 3.}

\item{mds_max_items}{Maximum candidate-pool items included in Plot 9 MDS.}

\item{path_proxy_max_items}{Maximum candidate-pool items represented in the
fast Plot 10 BEFORE proxy.}

\item{include_interactive}{Logical; generate the interactive Plot 9 widget.}

\item{progress}{Logical; print each plot name and elapsed generation time.}
}
\value{
Named list of \code{ggplot} and \code{plotly} objects, including
\code{p10a_path_before}, \code{p10b_path_after}, and
\code{plot_summary_of_results}. The opt-in \code{\link[=the_coolest_plot_ever]{the_coolest_plot_ever()}} plot is
intentionally not generated by this wrapper.
}
\description{
Generate all SEMANTICA diagnostic plots
}
\examples{
\dontrun{
plots <- semantica_plot_all(
  result = result,
  cosine_sim_matrix = cosine_sim_matrix,
  df = item_metadata,
  include_interactive = TRUE,
  before_path_model = "proxy",
  save = FALSE
)
}
}
