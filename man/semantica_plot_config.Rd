% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pipeline.R
\name{semantica_plot_config}
\alias{semantica_plot_config}
\title{Configure plotting for the configurable pipeline}
\usage{
semantica_plot_config(
  level = c("summary", "full", "none"),
  interactive_mode = c("2d", "3d"),
  save = FALSE,
  out_dir = "semantica_plots/",
  device = "png",
  width = 12,
  height = 8,
  dpi = 180,
  before_path_model = c("proxy", "refit"),
  before_path_refit_max_items = 60L,
  network_max_items = 200L,
  mds_max_items = 250L,
  path_proxy_max_items = 150L,
  include_interactive = TRUE,
  progress = NULL
)
}
\arguments{
\item{level}{\code{"none"}, \code{"summary"}, or \code{"full"}. \code{"summary"} stores the
five core report plots; \code{"full"} requests the complete visualization set.}

\item{interactive_mode}{\code{"2d"} or \code{"3d"} for the interactive MDS plot.}

\item{save}{Save generated plots.}

\item{out_dir}{Output directory.}

\item{device, width, height, dpi}{Saved-plot device controls.}

\item{before_path_model}{Method used for the full-level BEFORE path panel.}

\item{before_path_refit_max_items}{Maximum item count allowed for an optional BEFORE full-pool refit.}

\item{network_max_items}{Maximum pool items rendered in semantic-network diagnostics.}

\item{mds_max_items}{Maximum pool items included in MDS diagnostics.}

\item{path_proxy_max_items}{Maximum pool items represented in the fast BEFORE path proxy.}

\item{include_interactive}{Logical; include interactive plotly diagnostics when requested.}

\item{progress}{Print per-plot progress; \code{NULL} follows \code{verbose}.}
}
\value{
A configuration list for \code{\link[=semantica_full_pipeline]{semantica_full_pipeline()}}.
}
\description{
Controls how high-level diagnostic plots are produced or saved. The default
\code{"summary"} level retains the core report set in the result object: the
summary, ACO fitness evolution, BEFORE/AFTER ESEM path views, and PFA
solution plot. For an already-computed result, call \code{plot(result)} for the
summary or \code{plot(result, which = "all")} for the complete plot set.
}
